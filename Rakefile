require './td_stats.rb'

namespace :aggregate do
  desc 'aggregate jobs from Treasure Data'
  task :jobs do
    aggregate
  end

  desc 'aggregate records from Treasure Data'
  task :records do
    save_records
  end

  desc 'get running jobs from Treasure Data'
  task :running_jobs do
    running_jobs
  end
end
